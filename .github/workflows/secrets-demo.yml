name: Use Secrets in Workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Use Secret
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "Secret was loaded"
          echo "$API_KEY"
